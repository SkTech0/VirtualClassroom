# VirtualClassroom.Tests.Unit

Backend unit tests (xUnit, Moq, FluentAssertions). For run commands and coverage see the **root [README.md](../README.md)** — **Testing** section.

Tests cover: Auth, Rooms, Pomodoro, Video handlers and validators; `ValidationBehavior`; `InMemoryAuthTokenService`.
