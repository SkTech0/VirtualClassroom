# Example secrets - DO NOT commit real values. Use:
#   kubectl create secret generic virtualclassroom-secrets --from-literal=...
# or apply this file after replacing placeholders and renaming to secrets.yaml (gitignored)
apiVersion: v1
kind: Secret
metadata:
  name: virtualclassroom-secrets
  namespace: virtualclassroom
type: Opaque
stringData:
  postgres-connection-string: "Host=postgres-service;Port=5432;Database=VirtualClassroom;Username=postgres;Password=CHANGE_ME"
  JwtSettings__SecretKey: "CHANGE_ME_MIN_32_CHARS_FOR_HS256"
  LiveKit__ApiSecret: "CHANGE_ME_LIVEKIT_SECRET"
# Optional: LiveKit__ApiKey if not in ConfigMap
#  LiveKit__ApiKey: "your-livekit-api-key"
